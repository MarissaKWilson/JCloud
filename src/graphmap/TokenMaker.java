package graphmap;
/**
 * Takes the output from JParser and creates tokens from the authors and variables
 * Iteratively goes through by author
 * 
 * @author M
 *
 */
public class TokenMaker {
	//store each commit here
	/*
	 * Constructor will take in the commits as parsed by JParser
	 * Then for each author for each commit will get add each item to the glyph graph
	 */
	public TokenMaker(){
		
	}
	/*
	 * Creates the tokens for each author, 
	 * passes tokens to GlyphGraph to be added to the graph
	 */
	public void makeToken(){
		//for each commit
			//for each string
				//Glyph g = new Glyph(string name)
	}
}
